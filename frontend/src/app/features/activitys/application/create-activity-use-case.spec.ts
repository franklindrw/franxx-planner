import { CreateActivityUseCase } from './create-activity-use-case';

describe('CreateActivityUseCase', () => {
  it('should create an instance', () => {
    expect(new CreateActivityUseCase()).toBeTruthy();
  });
});
